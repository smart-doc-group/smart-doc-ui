/**
 * be used to get space string with specific length.
 * @param {number} length the length of space the function returns, default is 1
 * @return {string} space string with specific length.
 */
export function sp(length: number = 1) {
  return ' '.repeat(length);
}
